<%= form_for([:admin, @landing]) do |form| %>
  <% if landing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(landing.errors.count, "error") %> prohibited this landing from being saved:</h2>

      <ul>
      <% landing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %><br>
    <%= form.text_field :title, id: :landing_title %>
  </div>

  <div class="field">
    <%= form.label :image %><br>
    <%= form.file_field :image, id: :landing_image %>
  </div>

  <div class="field">
    <%= form.label :heading %><br>
    <%= form.text_field :heading, id: :landing_heading %>
  </div>

  <div class="field">
    <%= form.label :body %><br>
    <%= form.text_area :body, id: :landing_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
