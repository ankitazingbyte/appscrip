<%= form_with(model: component, local: true) do |form| %>
  <% if component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component.errors.count, "error") %> prohibited this component from being saved:</h2>

      <ul>
      <% component.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :heading %>
    <%= form.text_field :heading, id: :component_heading %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :component_image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :component_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
