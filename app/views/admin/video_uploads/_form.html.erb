<%= form_for([:admin, @video_upload]) do |form| %>
  <% if video_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_upload.errors.count, "error") %> prohibited this video_upload from being saved:</h2>

      <ul>
      <% video_upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :video %>
    <%= form.text_field :video, id: :video_upload_video %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
